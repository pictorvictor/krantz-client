import {StyleSheet} from 'react-native';

export const KitchenDetailsPageStyles = StyleSheet.create({
  container: {
    position: 'relative',
  },
  backButton: {position: 'absolute', top: 10, left: 10, zIndex: 2},
});
