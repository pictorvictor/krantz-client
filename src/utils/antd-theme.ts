import theme from './theme';

const antdTheme = {
  primary_button_fill: theme.palette.primary,
  primary_button_fill_tap: theme.palette.primaryDark,
  radius_xs: 15,
  radius_sm: 15,
  radius_md: 15,
  radius_lg: 15,
};

export default antdTheme;
